version: "3.9"
networks:
  globalnet:
    external: true

services:
  talentBlendr:
    image: pitchnhire/talentblendr:develop
    container_name: talentblendr
    restart: unless-stopped
    networks:
      - globalnet
    ports:
      - 7474:80
